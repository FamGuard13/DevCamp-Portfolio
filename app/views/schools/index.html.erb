<div class="grid-sort">
  <% @schools.each do |school| %>
    <%= link_to(school_show_path(school)) do%>
        <ul class="school">
          <li>
            <%= school.name %>
          </li>

          <li>
            <%= school.location %>
          </li>

          <li>
            <%= image_tag(school.img) if school.img%>
          </li>
        </ul>
    <% end %>
  <% end %>
</div>